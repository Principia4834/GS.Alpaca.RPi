/* Additions for native/linux/webview_shim.c: typedef NavigationStateChangedCallback, register function, notify function, on_load_changed connection */

// Example stub - actual content should include proper function implementations based on your existing code structure.
/*
typedef void (__cdecl *NavigationStateChangedCallback)(int canGoBack, int canGoForward, void* userData);

void register_navigation_state_callback(NavigationStateChangedCallback cb, void* userData) {
    // Store callback and userData
}

void notify_navigation_state_changed(/* parameters */) {
    // Call the registered callback with the current state
}

// Connect to the load changed event and call notify
*/
